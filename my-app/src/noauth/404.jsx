import Logo from "../auth/elements/Logo";
import './NotFound.css';

export const NotFound = () => {
    return (
        <section className="NotFound">
            <h1>404! not found</h1>
            <Logo />
        </section>
    )
}